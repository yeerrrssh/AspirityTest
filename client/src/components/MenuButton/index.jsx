import React from 'react';
import {ReactComponent as Menu} from "../../img/button.svg";

const MenuButton = () => {
    return (
        <div className='cursor-pointer'>
            <Menu/>
        </div>
    );
};

export default MenuButton;